#include <stdio.h>
#include <stdbool.h>

int main() {
	int numbers[] = {2,5,6,1,2};
	int length = sizeof(numbers) / sizeof(numbers[0]);
	int i;

	for (i = 0; i < length; i++) {
		printf("%d\n", numbers[i]);
	}

	return 0;
}

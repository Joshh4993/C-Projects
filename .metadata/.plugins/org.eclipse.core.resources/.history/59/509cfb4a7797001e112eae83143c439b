#include <stdio.h>

int main() {
	int myNum = 15;
	char myLetter = 'C';

	printf("My number is %d and my letter is %c", myNum, myLetter);
	for (int i = 0; i < 10; ++i) {
		myNum++;
		printf("My number is now %d", myNum);
	}
	return 0;
}

#include <stdio.h>

int main() {
	int myNum = 15;
	char myLetter = 'C';

	printf("My number is %d and my letter is %c\n\n", myNum, myLetter);
	for (int i = 0; i < 10; ++i) {
		myNum++;
		printf("My number is now %d\n", myNum);
	}
	return 0;
}

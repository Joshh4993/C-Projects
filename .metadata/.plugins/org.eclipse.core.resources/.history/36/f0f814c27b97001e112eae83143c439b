#include <stdio.h>
#include <stdbool.h>

int main() {
	int numbers[] = {2,5,6,1,2};

	printf("%d\n", numbers[0]);

	numbers[2] = 10;

	int length = sizeof(numbers) / sizeof(numbers[0]);

	printf("%d", length);

	return 0;
}

#include <stdio.h>
#include <stdbool.h>
#include <string.h>

int addition(int, int);

int main() {
	int a = 9;
	int b = 12;

	int result = addition(a,b);
	printf("Result is: %d", result);

	return 0;
}

int addition(int x, int y) {
	return x + y;
}
